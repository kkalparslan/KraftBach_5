package Day_1_BenimCalismalarim;

public class ArrayCalismaOrtakGrup {
    public static void main(String[] args) {

        /**
         * Create a method in this class that takes a List of strings as the only argument.
         * Each string in this list will be a single word containing either alphabets a through
         * z or digits 0 through 9 (for example, “abcd”, “Pune”, “623”, “Mumbai”, “10”, etc.
         */
//        ArrayList<Integer> arrayList = new ArrayList<Integer>();
//        arrayList.add(1);
//        arrayList.add(2);
//        arrayList.add(3);
//
//        Integer[] array = arrayList.toArray(new Integer[arrayList.size()]);
//        System.out.println("arrayList = " + arrayList);
//        System.out.println("Arrays.toString(array) = " + Arrays.toString(array));

//        List<Integer> list = new ArrayList<Integer>();
//        list.add(1);
//        list.add(2);
//        list.add(3);
//
//        Integer[] array = list.toArray(new Integer[list.size()]);
//        System.out.println("list = " + list);
//        System.out.println("Arrays.toString(array) = " + Arrays.toString(array));

        /**
         * String str="125klm89dyhr41"; bu stringi harf ve rakamları ayırarak bir array e yazınız.
         */

       /* StringWorkings str = "125klm89dyhr41";
        char[] charArray = str.toCharArray();
        List<Character> letters = new ArrayList<>();
        List<Character> digits = new ArrayList<>();

        for (char c : charArray) {
            if (Character.isLetter(c)) {
                letters.add(c);
            } else if (Character.isDigit(c)) {
                digits.add(c);
            }
        }
        Character[] lettersArray = letters.toArray(new Character[letters.size()]);
        Character[] digitsArray = digits.toArray(new Character[digits.size()]);


    }
    public static void letterOrDigit(StringWorkings[] arr){
        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr [i].length(); j++) {
            }
        }
    }*/
    }
}
